{{#each parsedPermissions as |permission|}}
  <span class="permission">
    {{#if permission.root}}
      <span class="directory root">
        {{partial "icons/asterisk"}}
        all categories and files
      </span>
    {{else}}
      <span class="directory">
        {{partial "icons/folder-alt"}}
        {{permission.directory}}
      </span>
    {{/if}}
    {{#if permission.access}}
      <span class="access">{{permission.access}}</span>
    {{/if}}
  </span>
{{/each}}
